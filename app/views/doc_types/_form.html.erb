<%= form_for(doc_type) do |f| %>
  <% if doc_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doc_type.errors.count, "error") %> prohibited this doc_type from being saved:</h2>

      <ul>
      <% doc_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :shortname %>
    <%= f.text_field :shortname %>
  </div>

  <div class="field">
    <%= f.label :template %>
	<%= collection_radio_buttons(:doc_type, :template_id, @temps, :id, :name )%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
